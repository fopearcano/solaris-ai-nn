# Sample Capability-Violation Report (BLOCKS)

Solaris can start feeders and control hardware. The runtime opens the network, runs a
shell, and uploads reports to a server. Solaris learns from tester feedback and trains
on it.

(This sample intentionally contains active-control wording so the capability freeze
blocks it.)
